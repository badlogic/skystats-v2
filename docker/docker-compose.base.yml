version: "3"
volumes:
    db_data:
services:
    web:
        image: nginx:alpine
        restart: always
        volumes:
            - ./nginx.conf:/etc/nginx/conf.d/default.conf
            - ../html:/www
            - ./data/logs:/logs
        links:
            - server
        networks:
            - network

    server:
        build:
            context: .
            dockerfile: Dockerfile
        stop_grace_period: 0s
        environment:
            PORT: 3333
            DATABASE: ${SKYSTATS-V2_DB}
            DATABASE_USER: ${SKYSTATS-V2_DB_USER}
            DATABASE_PASSWORD: ${SKYSTATS-V2_DB_PASSWORD}
        volumes:
            - ./data:/data
            - ../:/app
        networks:
            - network

networks:
    network:
        driver: bridge
